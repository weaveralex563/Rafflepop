kind = "web"
previewPath = "/"
title = "AdJackpot"
version = "1.0.0"
id = "artifacts/lottery-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25222

[services.development]
run = "pnpm --filter @workspace/lottery-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/lottery-app", "run", "build" ]
publicDir = "artifacts/lottery-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25222"
BASE_PATH = "/"
